---
name: 会议记录转会议纪要和设计任务书
version: 1.0.0
title: 录音文字版转会议纪要与设计任务书
category: 通用办公
agent_created: true
disable: false
tags:
  - 会议纪要
  - 设计任务书
  - 录音整理
  - 文档生成
  - 忠实引用
description: 将一份精校后的会议文字记录，按参考格式转换成会议纪要，同步输出为设计任务书的doc文件。
---

# 会议记录转会议纪要和设计任务书 — 文字记录 → 会议纪要 + 设计任务书

## 一句话定位
把会议录音的**文字转写版**整理成 **1 份精炼会议纪要 + N 份设计任务书**（每设计议题一份）。
核心纪律：**忠实引用、不补充、引用标记**。

## 何时触发
- 用户给出会议录音/会议记录文字版（.doc/.docx/.pdf/.txt/.md），要求"整理成会议纪要""出设计任务书""按参考格式整理"
- 用户说"把这个录音文字版整理一下""按 E:\资料库 的格式出纪要和任务书""从会议里提炼设计需求"
- 注意：输入已是**文字版**，不要加载 ASR 转写技能

## 输入参数（开场一次性确认，用 AskUserQuestion）
- `input_transcript`（必填）：文字版录音文件路径
- `reference_dir`（可选）：参考纪要目录，用于学习格式与语言习惯
- `output_dir`（可选）：输出目录，默认与 `input_transcript` 同目录
- `scope`：仅设计议题 / 设计议题+其他事项
- `head_info`：地点 / 主持 / 出席人员（缺省标"待补充"）

## 标准工作流（5 步）

### 第 1 步：解析转录稿 → 校对底稿
运行随附脚本，按扩展名自动选解码方式：
```bash
PY="<managed python venv>/Scripts/python.exe"
"$PY" "<skill_dir>/scripts/extract_transcript.py" \
  --input "<input_transcript>" \
  --out "<output_dir>/校对底稿.txt"
```
- `.doc`（旧版/WPS 二进制）→ UTF-16LE 解码后按 CJK 正则抽片段，过滤元数据
- `.docx` → zipfile 读 `word/document.xml` 抽 `<w:t>`
- `.pdf` → pypdf / pdfminer 文本抽取
- `.txt/.md` → 直接读
输出纯文本「校对底稿.txt」作为引用溯源依据，**通读全文**。

### 第 2 步：学习参考格式（可选）
若提供 `reference_dir`，用同一脚本解析其中 1–2 份 .docx/.doc，提取：
- 头部字段结构（会议主题 / 时间 / 地点 / 主持 / 出席人员 / 会议内容）
- 分条风格（一、二、 / （一）（二） / 结论附后）
- 语言语气（正式、精炼、结论导向）
作为输出模板。无参考目录时采用下方「默认纪要结构」。

### 第 3 步：萃取议题
通读底稿，识别与"设计"相关的议题（雕塑、办公室装修、景观、标识、展厅等）。
与设计无关的闲聊（结算、人员、日常）按 `scope` 处理：
- **仅设计议题**：不写入
- **设计议题+其他事项**：以"其他事项"摘要简列

### 第 4 步：生成文档
`from scripts.docx_helper import *` 复用排版函数（`new_doc / add_heading / add_field / add_para / add_mixed`）生成 docx。
- **会议纪要**：头部 + 分条纪要（每议题一节，下设背景/范围/标准/功能/成本/工期/下一步）+ 补充意见 + 备注 + 会签
- **设计任务书（每议题一份）**：项目名称 / 项目背景 / 设计要求 / 成本控制 / 深化制作 / 待确认事项 / 说明

**引用纪律（硬性，违反即失败）**：
- 原文重要引用 → 用「」包裹，紧跟"（原文引用）"；排版用楷体、蓝色 `RGB(0x1F,0x4E,0x79)`
- 改写/概括 → 不加引号，用仿宋、灰色 `RGB(0x2E,0x2E,0x2E)`；文末"说明"统一声明：「」内为会议原话引用（ASR 转写，个别字词可能有误），其余为整理概括
- 会议**未提及**的字段（尺寸、消防、机电、报建、工期等）→ 在"待确认事项"中标"（会议未提及，需另行确定）"，**绝不自行填充**

### 第 5 步：交付
落盘 `output_dir`，用 `present_files` 展示（会议纪要 + N 份任务书）。

## 默认纪要结构（无参考目录时）
```
[居中标题] 会 议 纪 要
（日期）
会议主题：
会议时间：
会议地点：（待补充）
会议主持：（待补充）
出席人员：（待补充）
会议内容：
一、议题A
  （一）背景与定位
  （二）范围与选址
  ……
二、议题B
补充意见：
备注：
出席人员会签：
```

## 注意事项
- `.doc` 为机器转写文字版，个别字词可能有误（同音误识，如"港工生"疑为"港中深"），保留原文字样并在备注声明
- 出席人员若无结构化名单，按对话中参与说话者填写并标注"（据录音对话整理，请以实际参会签名为准）"
- 引用中如需修正转写误差，可在 Word 中全文替换，或提示用户统一处理
- 不加载 ASR 转写技能——本 skill 输入已是文字版
